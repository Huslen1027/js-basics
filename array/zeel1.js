function loan(salary, insurance, inpersontype, dept, id) {
  if (
    (salary > 2000000 &&
      insurance == "surance" &&
      inpersontype == "inperson") ||
    (inpersontype == "cred" &&
      inpersontype == "cred" &&
      dept < 1000000 &&
      id == "passport") ||
    id == "identation card" ||
    id
  ) {
    console.log("yes");
  } else console.log("no");
}
let ar = loan(2000002, "surance ", "inperson", 500000, "passport, ");
